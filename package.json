{
  "dependencies": {
    "api": "^6.1.1",
    "express": "^4.18.2",
    "nodemon": "^3.0.2"
  },
  "scripts": {
    "dev": "nodemon app",
    "start": "node app",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
